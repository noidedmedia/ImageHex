<%# Generic comments partial for use with more than just image pages. %>
<% if comments.empty? %>
  <p class="no-comments"><%= t(".no_comments_yet") %>
<% else %>
  <%= render partial: "shared/comment", collection: comments.for_display %>
<% end %>

