<li class="dashboard-listing-item">
  <%= link_to listing do %>
    <%= image_tag(listing.images.first.try(:f).try(:url, :large) || 
                  "empty-collection.svg") %>
  <% end %>
  <div class="listing-bottom-bar">
    <div class="listing-name"><%= listing.name %></div>
  </div>
</li>
