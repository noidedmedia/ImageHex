<%= form_for @offer, html: {id: "commission-offer-form", multipart: true} do |f| %>

  <%= f.label :description %>
  <%= f.text_area :description %> 
  <div id="offer-form-react-container"
    data-product-id="<%= @offer.commission_product_id %>">
    <span>
      One moment...
    </span>
    <noscript>
      This form requires JavaScript to work. Please enable it!
    </noscript>
  </div>
  <%= f.submit %> 
<% end %>
