<%= form_for @offer, html: {class: "commission-offer-form", id: "commission-offer-form", multipart: true} do |f| %>
  <%= react_component("CommissionOfferForm", render(file: "commission_offers/show.json.jbuilder")) %> 
  <%= f.label :description %>
  <%= f.text_area :description %>

<% end %>
