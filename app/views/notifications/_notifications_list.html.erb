<% notifications.includes(:subject).each do |note| %>
  <div class="notifications-list-item">
    <%= link_to note.subject do %>
      <%= note.message %>
      <%= time_ago_in_words(note.created_at) %> ago
    <% end %>
  </div>
<% end %>
