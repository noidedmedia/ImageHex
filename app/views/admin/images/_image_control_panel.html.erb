<%= image_tag i.f :medium %>
<% i.image_reports.each do |i| %>
  <%= i.reason %>
  <%= i.message %>
<% end %>
  <%= button_to "Delete Image", admin_image_path(i), method: :delete %>
  <%= button_to "Absolve Image", absolve_admin_image_path(i)%>
