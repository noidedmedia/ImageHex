<!DOCTYPE html>
<html>
<head>
  <title>ImageHex</title>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','//www.google-analytics.com/analytics.js','ga');
  ga('create', 'UA-30487635-3', 'auto');
  ga('send', 'pageview');
  </script>

  <%# Favicon %>
  
  <%# 16px and 32px .ico favicons %>
  <link rel="shortcut icon" href="/favicon.ico" />
  <link rel="icon" sizes="16x16 32x32" href="/favicon.ico">

  <%# 16px and 32px favicons, just in case someone is using some absurdly old browser. %>
  <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16.png"/>
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png"/>

  <%# 64px icon, used by most desktop browsers. %>
  <link rel="icon" type="image/png" sizes="64x64" href="/favicon-64.png"/>
  
  <%# For iPhone with high-resolution Retina display running iOS ≥ 7. %>
  <link rel="apple-touch-icon" sizes="120x120" href="/favicon-120-mobile.png">
  
  <%# For iPad retina and general Android device compatibility. %>
  <link rel="apple-touch-icon" href="/favicon-152.png">

  <%# Viewport meta tag %>
  <meta name="viewport" content="width=device-width, user-scalable=no">

  <%# UTF-8 Character Set %>
  <meta charset="utf-8">

  <meta name="description" content="ImageHex is a place to search, share, and commission images and other artwork. We endeavour to be the best directory of images on the internet.">
</head>

<body>
  <%= render 'layouts/header' %>
  <% if notice %>
    <div class="alert alert-success"><p><%= notice %></p></div>
  <% end %>
  <% if info %>
    <div class="alert alert-info"><p><%= info %></p></div>
  <% end %>
  <% if alert %>
    <div class="alert alert-danger"><p><%= alert %></p></div>
  <% end %>
  <% if warning %>
    <div class="alert alert-warning"><p><%= warning %></p></div>
  <% end %>

  <div id="masthead">
    <%= yield %>
  </div>

  <%= render 'layouts/footer' %>

  <%# This script can be parsed by web crawlers to enable Google's in-results search box. %>
  <script type="application/ld+json">
  {
     "@context": "http://schema.org",
     "@type": "WebSite",
     "url": "http://www.imagehex.com/",
     "potentialAction": {
       "@type": "SearchAction",
       "target": "http://www.imagehex.com/search?query%5B%5D={search_term_string}",
       "query-input": "required name=search_term_string"
     }
  }
  </script>
</body>
</html>
